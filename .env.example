# Copiar a .env y configurar en EasyPanel
# IMPORTANTE: incluir /going antes de ? para usar la base de datos correcta
# Ejemplo: mongodb://user:pass@host:port/going?authSource=admin
MONGODB_URI=
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Cron (recordatorios de pedidos pendientes). Vercel lo envía automático; para cron externo: Authorization: Bearer <valor>
CRON_SECRET=
# Opcional: URL base para el cron interno. Si no se define, usa NEXTAUTH_URL en producción.
# En Docker/Easypanel, 127.0.0.1 suele fallar (ECONNREFUSED) → usar la URL pública.
# CRON_SELF_URL=https://goingsm.com

# PWA Push Notifications (generar con: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:support@going.app

# Storage (MinIO/S3) - pruebas de recogida/entrega
# Opción A: MINIO_*
# MINIO_ENDPOINT=minio
# MINIO_PORT=9000
# MINIO_USE_SSL=false
# MINIO_ACCESS_KEY=
# MINIO_SECRET_KEY=
# MINIO_BUCKET=going
# MINIO_PUBLIC_URL=
# MINIO_REQUEST_TIMEOUT_MS=30000

# Opción B: STORAGE_* (compatible con n8n, etc.)
# STORAGE_ENDPOINT=http://n8n_minio:9000
# STORAGE_ACCESS_KEY_ID=
# STORAGE_SECRET_ACCESS_KEY=
# STORAGE_BUCKET_NAME=going

# Mapbox (geocoding: dirección → lat/lng). Token secreto (sk.) para servidor.
MAPBOX_ACCESS_TOKEN=
